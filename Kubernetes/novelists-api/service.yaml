apiVersion: v1
kind: Service
metadata:
  name: novelists-api
  labels:
    app: golang
    name: novelists-api
    role: api
spec:
  ports:
    # the port that this service should serve on
  - port: 80
  selector:
    name: novelists-api
